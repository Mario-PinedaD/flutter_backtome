import 'package:flutter/material.dart';

class Pageuser extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Esta es la página de usuario desde una nueva clase"),
    );
  }
}
